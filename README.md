# ai_news_clustering
This notebook collects real-time AI news articles using NewsData.io, preprocesses the text, applies TF-IDF vectorization, selects an optimal k value using silhouette scores, and performs K-Means clustering to identify major themes in AI news coverage.
